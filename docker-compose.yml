services:
  db:
    image: mariadb:latest
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - data-db:/var/lib/mysql
    restart: unless-stopped

  php:
    build:
      context: .
      dockerfile: php.Dockerfile
    volumes:
      - ./app:/var/www/html
    depends_on:
      - db
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - 80
      - 443
    volumes:
      - ./app:/var/www/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php
    restart: unless-stopped

  ftp:
    image: fauria/vsftpd
    ports:
      - 21:21
      - 20:20
      - 21100-21110:21100-21110
    volumes:
      - ./app:/home/vsftpd/ftpuser
      #- ./vsftpd/vsftpd.conf:/etc/vsftpd/vsftpd.conf:ro
    environment:
      FTP_USER: "ftpuser"
      FTP_PASS: "ftppassword"
      PASV_ENABLE: "YES"
      UMASK: "022"
      LOCAL_UMASK: "022"
      FTP_UID: "33" 
      FTP_GID: "33"
      VIRTUAL_ROOT: "/home/vsftpd/ftpuser"
      FILE_OPEN_MODE: "0644"
    restart: unless-stopped

volumes:
  data-db:
