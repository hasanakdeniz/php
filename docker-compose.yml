services:
  db:
    image: mariadb:latest
    environment:
      MYSQL_ROOT_PASSWORD: ${PROJECT_NAME}-${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${PROJECT_NAME}-${MYSQL_DATABASE}
      MYSQL_USER: ${PROJECT_NAME}-${MYSQL_USER}
      MYSQL_PASSWORD: ${PROJECT_NAME}-${MYSQL_PASSWORD}
    volumes:
      - data-db:/home
      - data-db:/var/lib/mysql
    networks:
      - lemp-network
    restart: unless-stopped
  
  php:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - data-php:/home
      - ./app:/var/www/html
    depends_on:
      - db
    networks:
      - lemp-network
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - 80
      - 443
    volumes:
      - data-nginx:/home
      - ./app:/var/www/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php
    networks:
      - lemp-network
    restart: unless-stopped

networks:
  lemp-network:
    driver: bridge

volumes:
  data-db:
  data-php:
  data-nginx:
